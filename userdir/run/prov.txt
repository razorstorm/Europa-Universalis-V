log = "===============================Running Prov.txt=========================================="
####################################################################################


####################################################################################
####################################################################################
# Continuous things to run for this campaign + Global Flags
####################################################################################

ROOT = {
    # add_country_modifier = {
    #     modifier = medium_crown_power_cheat
    #     years = -1
    # }
    
    ######
        
    # c:SOI = {
    #     cancel_subject = c:NQS
    # }
    # c:AAA96 = {
    #     every_owned_location = {
    #         change_location_controller = c:GLH
    #     }
    # }
}

####################################################################################
# Ceding Locations
####################################################################################

ROOT = {
    location:neustadt = {
        change_location_owner = c:HUN
        # remove_location_modifier = local_crown_power_cheat
    }
    location:pavlovskaya.province = {
        change_province_owner = c:CRI
    }
}

####################################################################################
# Country Fixes
####################################################################################

ROOT = {
    c:CRI = {
        annex_country = {
            country = c:GAZ
            transfer = no
        }
    }
    c:SCA = {
        set_country_rank = country_rank:rank_empire
    }

    # Cultures
    add_accepted_culture = culture:crimean
}

####################################################################################
# Location Fixes
####################################################################################

ROOT = {
    every_owned_location = {
        limit = {
            OR = {
                NOT = {
                    has_location_modifier = local_crown_power_cheat
                }
                NOT = {
                    province = {
                        has_province_modifier = army_hub
                    }
                }
            }
        }

        if = {
            limit = { NOT = { has_location_modifier = local_crown_power_cheat }

            add_location_modifier = {
                modifier = local_crown_power_cheat
                years = -1
            }
        }

        # province = {
        #     if = {
        #         limit = { NOT = { has_province_modifier = army_hub } }
        #         add_province_modifier = {
        #             modifier = army_hub
        #             years = -1
        #         }
        #     }
        # }
        
        add_loc_mod_once = {
            modifier = local_crown_power_cheat
            years = -1
        }
        province = {
            add_prov_mod_once = {
                modifier = army_hub
                years = -1
            }
        }
    }
    
    every_owned_location = {
        convert_fraction_of_pops = {
            religion = lutheran
            fraction = 1
        }
    }
}
    
####################################################################################
# Subject  Fixes
####################################################################################

every_subject = {
    limit = {
        NOT = {
            religion = religion:lutheran
        }
    }
    change_religion = religion:lutheran
    convert_fraction_of_pops = {
        religion = lutheran
        fraction = 1
    }
}

####################################################################################
# Auto Infra Building
####################################################################################

# Build infrastructure
build_all_gov_infrastructure = yes
destroy_harmful_estate_buildings = yes

