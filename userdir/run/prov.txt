log = "===============================Running Prov.txt=========================================="
####################################################################################
# Configs
set_variable = {
	name = auto_convert_pops
	value = no
}
remove_variable = auto_convert_pops

# 1) Fight ottos
# DONE 2) Reforms and fix social values
# DONE 3) Test add_loc_mod_once -> shit don't work but found a cleaner way
# GAVE UP 4) Religious reforms
# DONE Exploring upgrading now 5) New regulars
# 6) Push to Land
# 7) Which crownpower cheat are we on?
# 8 implmenent fascism

####################################################################################
####################################################################################
# Continuous things to run for this campaign + Global Flags
####################################################################################
ROOT = {
	c:TUR = {
		break_alliance = {
			target = c:MAM
		}
	}
	c:MOR = {
		break_alliance = {
			target = c:MAM
		}
    }
}

####################################################################################
# Ceding Locations
####################################################################################
ROOT = {
	# location:hanau = {
	#     change_location_owner = c:ETH
	# }
	# location:rudnya_pontic.province = {
	#     change_province_owner = c:LIT
	# }
}

####################################################################################
# Country Fixes
####################################################################################
ROOT = {
	# c:ETH = {
	#     annex_country = {
	#         country = c:NQS
	#         transfer = no
	#     }
	# 
}


# Mil
every_country_sub_unit = {
    limit = {
        unit = {
            experience_percentage > 999
        }
        
    }
    add_subunit_experience = 100
}


####################################################################################
# Location Fixes
####################################################################################
ROOT = {
    add_provincial_army_hubs_to_all_provinces = yes
    add_local_crown_power_to_all_locations = yes
}

####################################################################################
# Subject Changes
####################################################################################
ROOT = {
}

####################################################################################
# Automatic Conversions
####################################################################################
ROOT = {
	if = {
		limit = {
			has_variable = auto_convert_pops
		}
		convert_fraction_of_pops = {
			religion = root.religion
			fraction = 1
		}
	}
	if = {
		limit = {
			has_variable = auto_convert_pops
		}
		every_subject = {
			limit = {
				NOT = {
					religion = root.religion
				}
			}
			change_religion = root.religion
			convert_fraction_of_pops = {
				religion = root.religion
				fraction = 1
			}
		}
	}
}

####################################################################################
# Auto Infra Building
####################################################################################
# Build infrastructure
build_all_gov_infrastructure = yes
destroy_harmful_estate_buildings = yes
expand_all_rgos = yes